test_pipeonce990:
  default_args:
    owner: 'tilin'
    start_date: 2023-05-24T12:00:00
    timezone: "Asia/Shanghai"
    retries: 1
    email: [
      'tilin@wish.com'
    ]
    email_on_failure: 'True'
    email_on_retry: 'False'
    catchup: 'True'
  schedule_interval: '@daily'
  concurrency: 3
  max_active_runs: 3
  default_view: 'tree' 
  orientation: 'LR'  
  description: 'mongo dump demo'
  tasks:        
    fpl.from_order:
      operator: airflow.providers.cncf.kubernetes.operators.kubernetes_pod.KubernetesPodOperator
      pod_template_file: '/opt/airflow/dags/repo/pod_template_files/payment_pipeline.yaml'
      get_logs: True
      labels: 
        task_id: "t1"
        dag_id: "mongo_dump_orders_bag_test"
      # cmds: [ 
      #   "python", 
      #   "/home/app/clroot/wishpost_payment_pipeline/wishpost_payment_pipeline/exp_entrypoint/dosql.py",
      # ]
      # arguments : [
      #   "--date={{ execution_date.strftime('%Y-%m-%dT%H:%M:%S%z') }}",
      #   "--biz_type=fpl",
      #   "--tbl_type=from_order",
      # ]